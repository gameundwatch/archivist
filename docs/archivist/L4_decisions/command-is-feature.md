# コマンド1本を feature 1枚とする

利用者が起動できるコマンドそれぞれに、feature / spec / design を1組ずつ持たせる。

feature は「利用者が名指しできるものを一つ」と定めてある。コマンドは
まさに名指しされるものなので、この定義に素直に従う。

要素ごとのスキルを archivist の内部部品として扱うと、利用者が直接
起動できるのに約束がどこにも書かれていない状態になる。単独で呼べるものは
単独で約束を持つ。
