# 取り込みの入力は決断記述が並ぶディレクトリ

取り込みのスキルが受け取るのは、一枚一文の決断記述が並んだディレクトリひとつ。
特定のツールの出力形式を入力条件にしない。

覆しにくい決定。

決断を実装から起こす工程は archivist の担当外で、別のツールが負う。その出力先を
入力条件に書くと、archivist がそのツールに依存する。

形式を問わない任意のテキストも認めない。読み取りが正しかったかを誰も検証できず、
取り込みの誤りが決断の誤りとして上層へ伝わる。

一枚一文という契約だけを要求すれば、外のツールの出力はたまたまそれを満たす、
という関係で済む。
