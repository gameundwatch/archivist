# 時間的な要素を文書に持たせない

版も、実装の順序も、文書には残さない。例外はファイル名の先頭の `_` だけ。

milestone は feature が消えても記録として残るため、「一つ下の層の要素が
消えたときに一緒に消えるか」という層の条件を満たさない。同じ理由で、
実装フロー図も層に属せない。

版の記録は git の tag と履歴が負う。文書が二重に持つと必ずずれる。

`_` だけを例外にするのは、実現先が実在するかどうかが文書の真偽に
関わるため。`ls` に印が出るので、現物と予定が一覧で読める。
