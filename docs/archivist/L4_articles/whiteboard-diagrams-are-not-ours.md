# 図は自前で描かない

文書の中の図は外部の実装に描かせる。生成時に取得して出力へ埋め込み、
archivist は図の記法を実装しない。

記法の追随は終わらない。自前で描くと、対象プロジェクトが書いた記法が
archivist の実装範囲を超えた時点で図が崩れる。崩れる範囲を archivist が
背負うことになる。

取得は生成時の一度きりにする。閲覧時に取りに行くと、出力を読める期間が
外部の可用性に縛られる。埋め込んだ後はネットワークを要らない。

取得できなかったときは図を出さない。代わりの描画を持たない。
