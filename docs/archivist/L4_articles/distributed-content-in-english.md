# 配布物は英語で書く

`skills/` 配下の `SKILL.md` と `TEMPLATE.md` は英語で書く。

配布物は誰の手にも渡る。読み手を書き手の言語に限定しない。

読み手はモデルでもある。同じ内容なら英語の方がトークンで軽く、指示への追従も安定する。
`SKILL.md` は人がほとんど読まないので、この利点だけが残る。

`docs/archivist/` はこの条項の対象外になる。archivist 自身の文書であり、
配布物ではない。言語は対象プロジェクトに合わせるという規則がそのまま当てはまる。
