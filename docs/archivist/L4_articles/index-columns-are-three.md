# 索引の列は path と title と refs の3つとする

`index.csv` は `path,title,refs` の3列とし、ヘッダ行を持たない。

行の中で他の列から導ける列を置かない。層はパスのディレクトリ名で決まる。列にすると
同じ値が1行の中に二度現れ、片方だけが古くなりうる。

入次数も列にしない。`refs` を数えれば出る。

値は全て引用符で囲み、値の中の `"` は `""` に倍にする。`refs` は参照先を `;` で
つなぐ。パスは `docs/archivist/` を基点に揃える。
