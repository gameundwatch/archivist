# 配置の切り替えは点の移動で行う

配置を切り替えるとき、点は新しい座標へ移動する。盤面を描き替えない。

どの点がどこへ行ったかが情報になる。被参照で芯にいた点が、層で並べたときにどの輪へ
移るかは、移動そのものが見せる。描き替えると、切り替えの前後は別の絵になり、
比較が記憶の勝負になる。

点の同一性が保たれることが条件で、移動の速さや軌跡は条件ではない。

移動の間も盤面は触れる。移動が終わるまで待たせない。
