# 盤面は掴んで動かす

視点を変える操作はドラッグとする。背景を掴むと盤面全体が動き、ノードを掴むと
そのノードだけが動く。

既定の盤面は画面に収まらないので、見る場所を変える手段が必ず要る。スクロールバーや
方向キーではなく掴む操作にするのは、机の上の盤面という比喩を壊さないため。
掴んだものがそのまま動く以上の説明を要らない。

ノードを動かせるのは、重なった点をどかして読むためで、配置を直すためではない。
動かした結果は保存しない。位置は配置から毎回導かれる。

自動で視点を追従させる機能を持つときは、手の操作が常に勝つ。掴んだ瞬間に追従は外れる。
