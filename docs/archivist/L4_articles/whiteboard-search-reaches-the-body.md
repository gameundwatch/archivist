# 検索は本文まで届く

盤面は検索を持つ。対象は表題とファイル名とパスに加えて、文書の本文まで含める。

どの内容がどこに書かれているかは、参照グラフからは読めない。位置と参照は文書どうしの
関係を示すもので、語の在り処は示さない。表題だけを引く検索は、表題を覚えている人にしか
効かない。

分かち書きに依存しない実装を使う。日本語の文書では語が空白で割れないため、
トークンに割ってから引く実装は語を取りこぼす。

検索結果から開いたものは、紙として開き、木の根になる。検索は盤面への入口であって、
別の閲覧面を作らない。
