# 印を置かず、実現先の実在は判定で読む

文書のファイル名に `_` を付けない。実現先が在るかどうかは、実現先そのものを見て
`archivist-check` が判定し、報告に現れる。

印は実現先の実在の写しであり、真ではない。その写しをファイル名に置くと、値が変わる
たびに改名が要る。改名は全ての流入リンクを書き換える唯一の操作で、参照を壊しうる
操作が写しの更新のためだけに存在することになる。

写しを持たなければ、判定は判定する者の手にだけある。一覧で読みたいときは、判定を
走らせるか、実現先を読む道具を別に立てる。

これにより、文書に時間的な要素は一つも残らない。
