# 生成される索引は層に属さない

参照の全体図を示す `docs/archivist/index.csv` は、どの層のノードにもしない。
文書から機械的に組み直せるもので、消えても何も失われないため。

層は文書どうしの参照順序を定める。索引は全ての文書を横から一覧するもので、
順序の中に位置を持たない。層に入れると、全ての層を指す一枚が生まれ、
参照が上から下へという向きが壊れる。

毎回まるごと書き直す。差分を当てない。
