# L5 は動かせない

`L5` は現に動いている実装である。既にある話について文書と実装が食い違ったなら、
直すのは文書の側になる。

実装は事実であり、文書は事実の記述になる。記述が事実に合わないとき、直すべきは記述。
逆にすると、文書に合わせるために動いているものを壊すことになる。

新しい話ではまだ実装が無く、そこへ向けて書く文書は事実ではなく予定になる。
その区別が `_` の印にあたる。
