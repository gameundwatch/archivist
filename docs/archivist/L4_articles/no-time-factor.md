# 時間的な要素を文書に持たせない

版も、実装の順序も、文書には残さない。例外を持たない。

milestone は feature が消えても記録として残るため、「一つ下の層の要素が
消えたときに一緒に消えるか」という層の条件を満たさない。同じ理由で、
実装フロー図も層に属せない。

版の記録は git の tag と履歴が負う。文書が二重に持つと必ずずれる。

実現先が実在するかどうかも、文書には持たせない。それは実現先の側の事実で、
読みたいときに判定して得る。
