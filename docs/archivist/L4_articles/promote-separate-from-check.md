# `_` を外す作業は check から分ける

archivist:check は読むだけで書かない。`_` の除去と流入リンクの書き換えは
archivist:promote が担う。

判定の難所は「実現先が実在するか」で、これは check が持つ。リネームと置換は
機械的なので、check の報告をそのまま食わせれば済む。

分ける理由は check の値打ちにある。何も変えずに現状を見たい場面は必ずあり、
読むつもりで走らせて名前が変わるなら、気軽に走らせられなくなる。
