# ホワイトボードは生成の後、バックグラウンドで配信する

`archivist-whiteboard` は出力を書いた後、`docs/archivist/` を手元だけに向けて配信する
サーバーをバックグラウンドで立て、開く URL を告げる。archivist の起動順から呼ばれた
ときも同じにする。

同じ場所をすでに配信しているサーバーがあれば、新しく立てずにそれを使う。止めるのは
人で、skill は止めない。

配信は盤面を開く手段であり、出力の成り立ちを変えない。出力は配信なしでも1枚で開ける。
