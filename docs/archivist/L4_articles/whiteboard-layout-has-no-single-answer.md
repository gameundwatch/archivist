# 盤面の配置に正解を1つ置かない

参照グラフの配置は交換可能な規則として複数持ち、どれかを唯一の正解としない。
既定は選ぶが、優劣は決めない。

配置ごとに読めるものが違う。力学配置はどの条項が効いているかを、層で固定した
配置は層飛ばしと逆流を、同心の配置は条項が全体を支えているかを見せる。
1つに絞ると、絞った時点で見えなくなる問いが出る。

配置は座標を返す関数1本として足せる。足すことが仕様変更にならない。

配置を切り替えても、開いている文書と注目しているノードは持ち越す。
見ているものが切れると、配置の違いが比較にならない。
